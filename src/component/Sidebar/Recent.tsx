import React from 'react'
// import { useGlobalState } from '../GlobalState/GlobalState';

const Recent = () => {
  // const { open, setOpen, close } = useGlobalState();

  return (
    <div className="text-xl md:text-2xl lg:text-3xl w-full max-w-[500px] p-4 mx-auto"
    // onClick={close}
    >
      Recent
    </div>
  )
}

export default Recent
